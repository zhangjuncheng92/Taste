<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:gravity="center"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/market_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/layout_padding_lss">

        <ImageView
            android:id="@+id/tab_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"
            android:layout_marginTop="5dp"
            android:contentDescription="@null"
            android:focusable="false" />

        <TextView
            android:id="@+id/tab_icon_count"
            android:layout_width="@dimen/layout_margin_l"
            android:layout_height="@dimen/layout_margin_l"
            android:layout_gravity="right|top"
            android:background="@drawable/tab_icon_count"
            android:gravity="center"
            android:textColor="@color/comm_white"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/tab_title"
        style="@style/main_tab_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/layout_margin_ls" />
</LinearLayout>